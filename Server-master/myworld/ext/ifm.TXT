Hi 王伟：

 

如果深圳那面方便的话，请让他们更新下informer库。

特别需要注意的是配置文件的路径问题。配置文件路径可以通过如下环境变量指定：

IFM_CONFIG_FILE_PATH

设置该环境变量的方法：

export IFM_CONFIG_FILE_PATH=/opt/informer/conf/config.ini

如果该环境变量不设置，默认会读取可执行文件（不是符号链接）所在目录下的conf/config.ini文件。

 

ps: 这个更新可能需要他们修改程序代码，而不仅仅是重新编译下。

 

附件分别是Linux 64位平台的库和32位平台的库。

